---
name: career-docs
description: "이력서·포트폴리오·자소서·제안서를 만들거나 고치거나 PDF로 뽑을 때 사용합니다. 서류용 HTML 조판, HTML→PDF·PPTX→PDF 변환, 경력과 실측 숫자 인용, AI 티 검사, 산출물 메타데이터 제거가 해당합니다. 숫자나 경력을 문서에 적기 전에 반드시 정본 파일을 먼저 확인합니다."
---

# 취업 서류 작업

이력서, 포트폴리오, 자소서, 제안서를 만들고 인쇄 품질 PDF로 내보내는 절차입니다.

## 시작 전에 반드시

경력 날짜나 성과 숫자를 한 글자라도 적기 전에 정본 파일을 읽습니다.
`references/facts-template.md`를 복사해 `facts.md`로 만들고, 서류에 들어가는
모든 수치의 출처와 측정 방법을 거기 적습니다. 서류는 그 파일만 따릅니다.

정본을 따로 두는 이유가 있습니다. 서류가 여러 벌로 늘어나면 오래된 파일을
복사하게 되고, 그 안의 틀린 날짜가 그대로 새 서류로 옮겨 갑니다. 한 번 나가면
회수가 안 됩니다.

숫자를 지어내지 않습니다. 안 센 것은 "안 셌습니다"라고 씁니다.
측정 방법이 적히지 않은 숫자는 서류에 넣지 않습니다.

## 파일 배치

작업 공간을 이렇게 둡니다.

| 위치 | 내용 |
|---|---|
| `facts.md` | 경력·학력·수치 정본. 다른 파일과 어긋나면 이쪽이 맞습니다 |
| `jd/<회사>.md` | 채용공고 원문 정리. 서류보다 먼저 만듭니다 |
| `docs/resume-<회사>.html` | 회사별 이력서. `styles/report.css` 사용 |
| `docs/portfolio-<회사>.html` | 회사별 포트폴리오 |
| `out/*.pdf` | 산출물 |

회사별 서류는 본체 하나를 복사해 회사에 맞게 고칩니다. 새 회사가 생기면 채용공고를
먼저 정리하고 그 다음에 서류를 씁니다. 공고를 안 읽고 쓴 서류는 어느 회사에도
맞지 않습니다.

## 조판

A4 세로 문서는 `styles/report.css`, 16:9 가로 문서는 `styles/deck.css`를 씁니다.
문서마다 새 CSS를 만들지 않습니다. 색과 서체가 서류마다 다르면 같은 사람이 낸
서류로 보이지 않습니다.

색과 서체는 지원하는 분야에서 끌어옵니다. 순수 회색은 고르지 않은 것으로 읽힙니다.
아래 조합은 피합니다. 지금 AI 생성 디자인이 몰려 있는 자리라서 서류에서 바로
알아봅니다.

- 크림색 배경(#F4F1EA 계열)에 세리프 제목, 테라코타 포인트
- 보라에서 파랑으로 가는 그라디언트 헤더
- Inter, Space Grotesk 기본값
- 전부 가운데 정렬, 모든 모서리에 같은 둥근 값
- 카드 왼쪽의 색 막대, 이모지 섹션 마커
- 근거 없는 번호 매김(01 / 02 / 03)

## 변환과 검사

명령 전문은 `references/typesetting.md`에 있습니다. 순서만 적습니다.

1. HTML은 시스템 Chrome 헤드리스로 PDF를 뽑습니다. `tools/html2pdf.sh`
2. PPTX는 LibreOffice로 변환하되 별도 사용자 프로필을 강제합니다. `tools/pptx2pdf.sh`
3. `pdffonts`로 서체 임베드를 확인합니다. Liberation이나 대체 서체가 보이면 폰트
   대체가 일어난 것이므로 다시 만듭니다.
4. `python3 tools/ai-tell-check.py <파일>`을 돌립니다. E 등급은 0이어야 합니다.
5. `python3 tools/strip-meta.py <파일>`로 생성 도구 표식을 지웁니다.

## 글

서류에서 AI가 쓴 인상은 내용이 아니라 패턴 반복에서 옵니다. 검사기가 잡는 것 외에
사람이 봐야 하는 것이 있습니다.

산문이 기본입니다. 세 줄로 될 것을 표와 목록으로 쪼개지 않습니다. 목록은 실제로
병렬 항목일 때만 씁니다. 한 문단에 굵은 글씨가 셋이면 강조가 아닙니다.
문장 길이를 섞습니다. 같은 길이 문장이 연달아 셋이면 기계 문장으로 읽힙니다.

슬라이드는 한 장에 주장 하나입니다. 불릿 다섯에 각각 완전한 문장을 넣는 구성이
가장 흔한 AI 티입니다. 실측치 없는 도식, 그러니까 화살표 세 개짜리 프로세스
다이어그램은 빈칸을 채우는 장치로 읽힙니다. 숫자가 있는 표나 실제 화면이 그 자리를
대신합니다.

보내기 전에 지웁니다. 무엇을 할지 예고하는 첫 문장, "도움이 되었으면" 류의 마지막
문장, 곁가지 사족, 정보 없는 완충어("아마도", "어느 정도")입니다.

## 내보내기 전 점검

- `ai-tell-check.py` 종료 코드 0, 줄표 0건
- 경력 날짜가 `facts.md`와 일치
- 확인 안 된 이력이 어디에도 없음
- 서로 다른 시스템의 숫자를 더해서 총계로 쓰지 않음
- 연락처 자리에 실제 값이 들어갔는지 확인, 없으면 그 줄을 지움
- PDF·PPTX 작성자 필드가 본인 이름, 생성 도구 표식 없음
- 커밋 메시지에 생성 도구 서명을 넣지 않음

## 지원 흐름

지원 버튼은 본인이 직접 누릅니다. 제출 직전 단계까지만 준비합니다.
지원 이력과 결과는 한 파일에 기록합니다. 어디에 무엇을 언제 냈는지 모르면
같은 회사에 두 번 내거나, 붙은 곳의 피드백을 다음 서류에 못 씁니다.
